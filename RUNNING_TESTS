Count has two distinct test suites - one for its basic functionality, and one to spec its Rails 3 integration.

Use:
`rake count` to run the main suite.
`rake rails` to run the Rails integration suite.
`rake` to run both back-to-back.
